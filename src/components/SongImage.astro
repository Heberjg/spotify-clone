---
import { Image } from "astro:assets";
const { image, alt, loading = 'eager' } = Astro.props;
---

<Image 
  src={image} 
  alt={alt} 
  width={300}  
  height={300}
  loading={loading}
  formats={['avif', 'webp']}
  quality={80}
/>